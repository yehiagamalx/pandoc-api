# API key for authenticating requests.
# If set, every request must include the header: X-API-Key: <value>
# Leave empty to disable authentication.
PANDOC_API_KEY=

# Port the Flask/Gunicorn server listens on inside the container.
PORT=5000
